const STORAGE_KEY='journal_notes_v1';const $=id=>document.getElementById(id);const grid=$('calendarGrid'),monthLabel=$('monthLabel'),selectedDateLabel=$('selectedDateLabel'),noteCount=$('noteCount'),notesList=$('notesList'),form=$('noteForm'),input=$('noteInput'),saveBtn=$('saveNoteBtn'),cancelEditBtn=$('cancelEditBtn'),exportBtn=$('exportBtn');let notes=loadNotes(),viewDate=startOfMonth(new Date()),selectedDate=toKey(new Date()),editingId=null;function loadNotes(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]')}catch{return[]}}function persist(){localStorage.setItem(STORAGE_KEY,JSON.stringify(notes))}function pad(n){return String(n).padStart(2,'0')}function toKey(d){return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`}function fromKey(k){const [y,m,d]=k.split('-').map(Number);return new Date(y,m-1,d)}function startOfMonth(d){return new Date(d.getFullYear(),d.getMonth(),1)}function fmtDay(k){return new Intl.DateTimeFormat('it-IT',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(fromKey(k))}function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function notesFor(k){return notes.filter(n=>n.date===k).sort((a,b)=>a.createdAt.localeCompare(b.createdAt))}function renderCalendar(){monthLabel.textContent=new Intl.DateTimeFormat('it-IT',{month:'long',year:'numeric'}).format(viewDate);grid.innerHTML='';const first=viewDate;const mondayIndex=(first.getDay()+6)%7;const start=new Date(first);start.setDate(first.getDate()-mondayIndex);const today=toKey(new Date());for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);const key=toKey(d),count=notesFor(key).length;const b=document.createElement('button');b.type='button';b.className='calendar-day'+(d.getMonth()!==viewDate.getMonth()?' outside':'')+(key===selectedDate?' selected':'')+(key===today?' today':'');b.dataset.date=key;b.innerHTML=`<span class="day-number">${d.getDate()}</span>${count?`<span class="note-dot">${count}</span>`:''}`;b.addEventListener('click',()=>{selectedDate=key;viewDate=startOfMonth(d);editingId=null;input.value='';saveBtn.textContent='Aggiungi nota';cancelEditBtn.hidden=true;renderAll()});grid.appendChild(b)}}function renderNotes(){selectedDateLabel.textContent=fmtDay(selectedDate);const dayNotes=notesFor(selectedDate);noteCount.textContent=`${dayNotes.length} ${dayNotes.length===1?'nota':'note'}`;if(!dayNotes.length){notesList.innerHTML='<div class="empty-notes">Nessuna nota per questo giorno.</div>';return}notesList.innerHTML=dayNotes.map(n=>`<article class="note-card"><p>${escapeHtml(n.text)}</p><div class="note-meta"><span>${new Intl.DateTimeFormat('it-IT',{hour:'2-digit',minute:'2-digit'}).format(new Date(n.updatedAt||n.createdAt))}</span><span class="note-actions"><button class="mini-button" data-edit="${n.id}">Modifica</button><button class="mini-button delete" data-delete="${n.id}">Elimina</button></span></div></article>`).join('');notesList.querySelectorAll('[data-edit]').forEach(b=>b.addEventListener('click',()=>beginEdit(b.dataset.edit)));notesList.querySelectorAll('[data-delete]').forEach(b=>b.addEventListener('click',()=>deleteNote(b.dataset.delete)))}function renderAll(){renderCalendar();renderNotes()}function beginEdit(id){const n=notes.find(x=>x.id===id);if(!n)return;editingId=id;input.value=n.text;saveBtn.textContent='Salva modifica';cancelEditBtn.hidden=false;input.focus()}function deleteNote(id){notes=notes.filter(n=>n.id!==id);if(editingId===id)cancelEdit();persist();renderAll()}function cancelEdit(){editingId=null;input.value='';saveBtn.textContent='Aggiungi nota';cancelEditBtn.hidden=true}form.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;const now=new Date().toISOString();if(editingId){const n=notes.find(x=>x.id===editingId);if(n){n.text=text;n.updatedAt=now}}else{notes.push({id:crypto.randomUUID?crypto.randomUUID():String(Date.now())+Math.random(),date:selectedDate,text,createdAt:now,updatedAt:now})}persist();cancelEdit();renderAll()});cancelEditBtn.addEventListener('click',cancelEdit);$('prevMonth').addEventListener('click',()=>{viewDate=new Date(viewDate.getFullYear(),viewDate.getMonth()-1,1);renderCalendar()});$('nextMonth').addEventListener('click',()=>{viewDate=new Date(viewDate.getFullYear(),viewDate.getMonth()+1,1);renderCalendar()});$('todayBtn').addEventListener('click',()=>{const t=new Date();selectedDate=toKey(t);viewDate=startOfMonth(t);renderAll()});exportBtn.addEventListener('click',()=>{const rows=[['Data','Nota']];[...notes].sort((a,b)=>a.date.localeCompare(b.date)||a.createdAt.localeCompare(b.createdAt)).forEach(n=>rows.push([n.date,n.text]));const csv='\uFEFF'+rows.map(r=>r.map(v=>'"'+String(v).replace(/"/g,'""')+'"').join(';')).join('\r\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`journal-${toKey(new Date())}.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)});renderAll();